const mysql = require('mysql2/promise'); require('dotenv').config(); async function main() { const conn = await mysql.createConnection({host: process.env.DB_HOST, user: process.env.DB_USER, password: process.env.DB_PASS, database: process.env.DB_NAME}); await conn.query('CREATE TABLE IF NOT EXISTS consumo_diario ( id_consumo INT UNSIGNED AUTO_INCREMENT PRIMARY KEY, id_suscriptor INT UNSIGNED NOT NULL, fecha DATE NOT NULL, id_dieta_comida INT UNSIGNED NULL, descripcion VARCHAR(255) NOT NULL, calorias DECIMAL(8,2) NULL, proteinas DECIMAL(8,2) NULL, grasas DECIMAL(8,2) NULL, carbohidratos DECIMAL(8,2) NULL, creado_en TIMESTAMP DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (id_suscriptor) REFERENCES suscriptores(id_suscriptor) ON DELETE CASCADE, FOREIGN KEY (id_dieta_comida) REFERENCES dieta_comidas(id_comida) ON DELETE SET NULL );'); console.log('Tabla consumo_diario creada con FKs correctas.'); conn.end(); } main();
